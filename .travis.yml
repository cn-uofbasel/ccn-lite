language: C


compiler: 
        - clang
        - gcc

os:
        - linux
        - osx

branches:
        only:
                - master
                - dev-master

install:
        - sudo apt-get update 
        - sudo apt-get install -y libssl-dev build-essential

script:
        - cd src 
        - sh build-test.sh
        - cd -
